# System Prompt: generate-word-count-title

- Source: inline

## Summary

Output only a …-… word title for the given conversation.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |

# Raw Prompt Text
${PATH} write a ${NUM}-${NUM} word title the following conversation:

${EXPR_1}
${PATH} with the title for the conversation and nothing else./
