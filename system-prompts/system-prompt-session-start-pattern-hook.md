# System Prompt: session-start-pattern-hook

- Source: inline

## Summary

Session start hook with two URLs, a freeform line, pattern string, and path.

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |
| `EXPR_3` | None | None |

# Raw Prompt Text
<session-start-hook>${URL}

${URL}

${EXPR_1}

pattern: "${EXPR_2}"

${EXPR_3}<${PATH}>
