# System Data Block: background-bash-with-settings

- Source: inline

## Summary

Formats background Bash task log with command, status, repeated URLs, and settings references

## Placeholder Hints (source-backed)

| Expression | Hint | Reference |
| --- | --- | --- |
| `EXPR_1` | None | None |
| `EXPR_2` | None | None |
| `EXPR_3` | None | None |

# Raw Prompt Text
${URL}

${URL}

Background Bash ${EXPR_1}

(command: ${EXPR_2})

(status: ${EXPR_3})

${NUM}

${URL}

${URL}

userSettings

projectSettings

localSettings
